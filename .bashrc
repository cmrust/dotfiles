# Aliases
alias ll="ls -l"
alias la="ls -al"
alias lt="ls -lt"
alias l="ls"

# Opens a SOCKS proxy to <home> on localhost:4444
alias proxytnnl="ssh -p24747 -D4444 chris@tnnl.in"

# Keeps ~/.bash_history file unbounded
export HISTSIZE=""
export HISTFILESIZE=""
