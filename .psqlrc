--store more historical queries
-- creates history files per connection like:
--  .psql_history-localhost-testdb
\set HISTFILE ~/.psql_history- :HOST - :DBNAME
-- change filesize, default is 500
\set HISTSIZE 2000

--tell how long queries take
\timing

--greeting
\echo '\nWelcome back, my chronicler.\n'
